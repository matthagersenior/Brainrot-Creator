{
  "name": "brainrot-creator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.mjs && node --check app-core.mjs && node --check functions/api/story.js && node --check functions/api/narrate.js && node --check functions/api/trends.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
